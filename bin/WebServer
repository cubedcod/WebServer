#!/bin/sh
this=WebServer
base=$(realpath ~/src/$this)
uid=$(id -u)
su -c "PATH=$PATH:$base/bin $this.net $uid" # configure network

cd $(realpath ~/web)
squid -f squid.conf                         # launch request-tagger
unicorn -N -l 127.0.0.1:8000 -l [::1]:8000 $base/config/$this.ru # run daemon
