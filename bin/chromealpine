#!/bin/sh
sudo mount -o remount,suid,exec /home/chronos/user
cd /home/chronos/user/alps
sudo mount -o bind .. home/user
sudo mount -o bind /dev dev
sudo mount -o bind /dev/pts dev/pts
sudo mount -o bind /dev/shm dev/shm
sudo mount -o bind /proc proc
sudo mount -o bind /sys sys
sudo mount -o bind /run run
sudo chroot . /bin/su -lc '~/src/WebServer/bin/webd' user
